<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Clubhouse</title>
</head>
<body>
  <%- include('header') %>
  
  <main>
    <% memberships.forEach(membership=> { %>
      <a href="/<%= membership.split('').filter(letter => letter !== ' ' ).map(letter => letter.toLowerCase()).join('') %>">
        <button class="group"><%= membership %></button>
      </a>
    <% }) %>
  </main>

</body>
</html>